import { ReferralDashboard } from "@/modules/account/components/ReferralDashboard";

export const metadata = {
  title: "Refer & Earn | CosstechCom",
  description: "Invite your friends to shop and earn rewards directly in your wallet.",
};

export default function ReferralPage() {
  return <ReferralDashboard />;
}
